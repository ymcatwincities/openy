services:
  logger.channel.dbsize:
    parent: logger.channel_base
    arguments: ['dbsize']
  dbsize.manager:
    class: Drupal\dbsize\DbSizeManager
    arguments: ['@database', '@entity_type.manager', '@entity_field.manager', '@logger.channel.dbsize']
