<?php

/**
 * @file
 * Contains yptf_kronos specific functions.
 */

/**
 * Implements hook_mail().
 */
function yptf_kronos_mail($key, &$message, $params) {
  switch ($key) {
    case 'yptf_kronos_reports':
      $message['subject'] = $params['subject'];
      $message['body'][] = $params['body'];
      break;
  }
}
