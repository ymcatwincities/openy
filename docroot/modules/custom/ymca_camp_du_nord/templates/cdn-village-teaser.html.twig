{#
/**
 * @file
 * Camp du Nord Village template.
 *
 * @ingroup themeable
 */
#}
<div class="cdn-village-teaser">
  <div class="available">{{ availability }}</div>
  <img src="{{ image }}" alt="{{ title }}" />
  <div class="info">
    <span class="title">{{ title }}</span>
    <span class="capacity">{{ capacity }}</span>
  </div>
</div>
