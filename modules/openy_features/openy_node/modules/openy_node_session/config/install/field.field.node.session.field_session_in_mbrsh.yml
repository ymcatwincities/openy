langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_session_in_mbrsh
    - node.type.session
id: node.session.field_session_in_mbrsh
field_name: field_session_in_mbrsh
entity_type: node
bundle: session
label: 'In membership'
description: 'Boolean field that helps determine if the session is included into membership package.'
required: false
translatable: true
default_value:
  -
    value: 0
default_value_callback: ''
settings:
  on_label: 'On'
  off_label: 'Off'
field_type: boolean
