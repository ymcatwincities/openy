langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_session_exclusions
    - node.type.session
  module:
    - datetime_range
id: node.session.field_session_exclusions
field_name: field_session_exclusions
entity_type: node
bundle: session
label: Exclusions
description: 'A date field that identifies dates that would normally have an instance of the session but won’t. Needs to be able to have multiple exclusions. Supports multiple values. Should be handled by a single date field with ''end date'' option enabled. Its widget should be adjust to not to show period end date, but show period end time (to keep period start/end date equal).'
required: false
translatable: true
default_value: {  }
default_value_callback: ''
settings: {  }
field_type: daterange
