{#

/**
 * @file
 *
 * Template file for generating the CSS file used for the menu item icons.
 */

#}

{% for icon in icons %}
  {% set path_image = base_path ~ icon.path %}
  .menu-icon-{{ icon.mlid }} .section-icon {
    background-image: url({{ path_image }});
  }
{% endfor %}
