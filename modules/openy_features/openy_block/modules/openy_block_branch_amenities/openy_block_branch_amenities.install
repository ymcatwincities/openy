<?php

/**
 * @file
 * OpenY Block Branch Amenities install file.
 */

/**
 * Implements hook_uninstall().
 */
function openy_block_branch_amenities_uninstall() {
  $modules_manager = \Drupal::service('openy.modules_manager');
  $modules_manager->removeEntityBundle('block_content', 'block_content_type', 'branch_amenities');
  $modules_manager->removeModuleConfigs('openy_block_branch_amenities');
}

/**
 * Update configs for Drupal Core upgrade.
 */
function openy_block_branch_amenities_update_8001() {
  $config_dir = drupal_get_path('module', 'openy_block_branch_amenities') . '/config/install/';
  // Update multiple configurations.
  $configs = [
    'core.entity_form_display.block_content.branch_amenities.default' => [
      'dependencies.config',
      'content',
    ],
  ];

  $config_updater = \Drupal::service('openy_upgrade_tool.param_updater');
  foreach ($configs as $config_name => $params) {
    $config = $config_dir . $config_name . '.yml';
    foreach ($params as $param) {
      $config_updater->update($config, $config_name, $param);
    }
  }
}
