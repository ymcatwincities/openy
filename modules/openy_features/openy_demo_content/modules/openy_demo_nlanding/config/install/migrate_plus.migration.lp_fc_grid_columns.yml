langcode: en
status: true
dependencies:
  enforced:
    module:
      - openy_demo_nlanding
id: openy_demo_lp_fc_grid_columns
migration_tags: {  }
migration_group: openy_demo_nlanding
label: 'Create grid column field collections for demo grid paragraphs'
migration_dependencies:
  required:
    - openy_demo_lp_paragraph_grid
source:
  plugin: embedded_data
  data_rows:
    -
      id: 1
      headline: 'Volunteers needed!'
      grid_id: homepage_grid
  ids:
    id:
      type: integer
process:
  field_name:
    plugin: default_value
    default_value: field_grid_columns
  host_type:
    plugin: default_value
    default_value: paragraph
  host_entity_id:
    plugin: migration
    migration: openy_demo_lp_paragraph_grid
    source: grid_id
  field_prgf_clm_headline: headline
destination:
  plugin: 'entity:field_collection_item'
