{#
/**
 * @file
 * GroupEx Pro week schedule template.
 *
 * Available variables:
 * - days: array of days with arrays of classes;
 * - pdf_href: string link to pdf file on groupex;
 *
 * @ingroup themeable
 */
#}
{% if instructor_location %}
  <div class="instructor-chosen">
    {{ instructor_location }}
  </div>
{% endif %}
<table{% if instructor_location %} class="instructor-display-mode"{% endif %}>
  <tbody>
    {% for day, classes in days %}
      <tr>
        <td class="caption" colspan="4">
          <a class="use-ajax" href="{{ classes.date_link }}">{{ classes.day_short }}</a>
        </td>
      </tr>
      {% for class in classes.classes %}
        {{ class }}
      {% endfor %}
      <tr>
        <td class="whitebar" colspan="3"></td>
      </tr>
    {% endfor %}
  </tbody>
</table>
