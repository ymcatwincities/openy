<div class="calc-summary">
  <div class="row row-eq-height">
    <div class="col-12 col-md-6 selected-branch calc-summary-col">
      <h2>{{ 'Your selected branch'|t }}</h2>
      <div class="selected-branch-map">{{ map }}</div>
      <div>{{ result.location }}</div>
    </div>
    <div class="col-12 col-md-6 selected-membership calc-summary-col">
      <h2>{{ 'Your selected membership'|t }}</h2>
      <div>{{ result.membership }}</div>
      {% if result.price %}
        <div class="price">
          <h4>{{ 'Membership price:'|t }}</h4>
          <h2>${{ result.price.monthly_rate }}/{{ 'month'|t }}</h2>
          <div>+ ${{ result.price.join_fee }} {{ 'one time joining fee'|t }}</div>
        </div>
      {% else %}
        <div class="price">
          <p>{{ 'Selected branch doesn\'t provide needed membership type or it wasn\'t configured yet. Please select other membership type or branch.'|t }}</p>
          <p>{{ 'Call to the branch to get additional details.'|t }}</p>
        </div>
      {% endif %}
    </div>
  </div>
</div>
